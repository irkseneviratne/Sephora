<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title -Welcome to Sephora</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" type="text/css" />

    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
    @*<script>
        $(function () {
            //myObj = $.getJSON('../JsonData/sampleData.json', function (data) {
            //    $("#name").text(data.data.id);
            //    $("#age").text(data.data.type);
            //    $("#attri").text(data.data.attributes.name);
            //});


            var myObj, i, j, x = "";
            myObj = {
                "data": [
                    { "id": "1", "type": "products", "attributes":["Fiesta", "Focus", "Mustang"] },
                    { "id": "2", "type": "products", "attributes":["320", "X3", "X5"] },
                    { "id": "3", "type": "products", "attributes": ["500", "Panda"] }
                ]
            }

            for (i in myObj.data) {
                x += "<h2>" + myObj.data[i].id + "</h2>";
                for (j in myObj.data[i].attributes) {
                    x += myObj.data[i].attributes[j] + "<br>";
                }
            }

            document.getElementById("demo").innerHTML = x;

        });
    </script>*@
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Sephora", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse pull-right">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Home", "Index", "Home")</li>
                    <li>@Html.ActionLink("About", "About", "Home")</li>
                    <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                </ul>
            </div>
        </div>
    </div>
    <div class="container-fluid">
        @RenderBody()
    </div>
    <hr />
    <div class="container-fluid">
        <div class="container">
            <footer>
                <p>&copy; @DateTime.Now.Year - Sephora</p>
            </footer>
        </div>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
